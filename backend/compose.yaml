services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=mydatabase'
      - 'POSTGRES_USER=$${spring.r2dbc.username}'
      - 'POSTGRES_PASSWORD=$${spring.r2dbc.password}'
    ports:
      - '5432'

services:
  postgres:
    image: 'postgres:16'
    environment:
      - 'POSTGRES_DB=lunchboard'
      - 'POSTGRES_USER=${DB_USER}'
      - 'POSTGRES_PASSWORD=${DB_PASSWORD}'
    ports:
      - '5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
